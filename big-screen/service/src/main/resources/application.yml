server:
  port: 8080
  servlet:
    context-path : /
spring:
  application:
    name: jiaoda-service
  mvc:
    async:
      request-timeout: 20000
  datasource:
    username: p_jiaoda_20220118 #用户名
    password: p_jiaoda_20220118 #密码
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://192.168.0.89:3306/p_jiaoda_20220118?allowMultiQueries=true&useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&zeroDateTimeBehavior=convertToNull
    initialSize: 5 #初始连接数
    minIdle: 5 #最大空闲
    maxActive: 20 #最大连接数
    filters: stat #druid过滤器 stat,wall
    useGlobalDataSourceStat: true #合并多个DruidDataSource的监控数据
    loginUsername: root # Druid页面登录用户名
    loginPassword: root # Druid页面登录密码
    resetEnable: false # Druid页面重置按钮是否失效
    allow:
    deny:

mybatis-plus:
  # 如果是放在resource目录 classpath:/mapper/*.xml
  mapper-locations: classpath*:mapper/**/*.xml
  #实体扫描，多个package用逗号或者分号分隔
  typeAliasesPackage: com.dfsoft.entity
  global-config:
    #主键类型  0:"数据库ID自增", 1:"用户输入ID",2:"全局唯一ID (数字类型唯一ID)", 3:"全局唯一ID UUID";
    id-type: 0
    #字段策略 0:"忽略判断",1:"非 NULL 判断"),2:"非空判断"
    field-strategy: 2
    #驼峰下划线转换
    db-column-underline: true
    #mp2.3+ 全局表前缀 mp_
    #table-prefix: mp_
    #刷新mapper 调试神器
    refresh-mapper: true
    #数据库大写下划线转换
    #capital-mode: true
    #逻辑删除配置（下面3个配置）
    logic-delete-value: 1
    logic-not-delete-value: 0

  configuration:
    #配置返回数据库(column下划线命名&&返回java实体是驼峰命名)，自动匹配无需as（没开启这个，SQL需要写as： select user_id as userId）
    map-underscore-to-camel-case: true
    cache-enabled: false
    #配置JdbcTypeForNull, oracle数据库必须配置
    jdbc-type-for-null: 'null'
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

## xss和sql过滤设置
xss-sql-config:
  # 忽略的拦截的路经,父目录直接配置 XXX/ 即可
  ignoreURL: /test/*,/test1/v1
  # 替换为空的字符
  sensitiveCharacter:
    - and
    - exec
    - insert
    - select
    - delete
    - update
    - count
    - '*'
    - '%'
    - chr
    - mid
    - master
    - truncate
    - char
    - declare
    - ;
    - or
    - '-'
    - '+'
  # 替换的字符
  replaceCharacter: { "[<]": '&lt;' , "[>]": '&gt;' , "[']": '& #39;' , script: '' }
